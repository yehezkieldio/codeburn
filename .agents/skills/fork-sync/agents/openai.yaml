interface:
  display_name: "Fork Sync"
  short_description: "Sync a fork with upstream"
  default_prompt: "Use $fork-sync to reconcile this fork with upstream, preserve local commits, and push the result to origin."
